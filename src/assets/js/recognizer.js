/**
 * Recognizes a given touch gesture
 * @param touchArray {Array} The array of finger positions
 * @return {String} The recognized gesture's identifier
 */
export const recognizeGesture = (touchArray) => {
	
	return "";
};
